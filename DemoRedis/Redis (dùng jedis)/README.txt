redis là một db nhưng được thiết kế tối ưu cho việc truy xuất bằng ram nên nhanh hơn.
thường dùng hset để lưu các obj vào bảng
set để lưu string
quy tắc đặt tên key để phân biệt table_name:primary_key